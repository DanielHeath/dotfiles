function pass {
  lpass show $( lpass ls -l --color=never | \
    selecta --search "$@" | \
    cut -d\[ -f 2 | \
    cut -d' ' -f 2 | \
    cut -d']' -f 1
  ) | grep '^Password' | cut -d' ' -f 2 | pbcopy
}
